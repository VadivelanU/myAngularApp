export interface ITemperatureDetails {
  temp: number;
  pressure: number;
  humidity: number;
  temp_min: number;
  temp_max: number;
}
export interface IWeatherDescription {
  main: string;
  description: string;
}

